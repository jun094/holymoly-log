import styled from '@emotion/styled';
import { SIZES } from 'styles/index';
import { FlexStyle } from 'ui/Flex/styles';

export const HeaderStyle = styled(FlexStyle)`
  height: 56px;
  padding: ${SIZES.container.paddingX};
`;
